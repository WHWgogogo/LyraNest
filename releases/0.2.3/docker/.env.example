# LyraNest 0.2.3 环境配置示例,复制为 .env 后按需修改。

# 在线镜像版本号(使用 ghcr 拉取模式时有效)
LYRANEST_VERSION=0.2.3

# 音乐下载插件凭据加密密钥(必填,否则插件市场不可用)
# 生成方式: openssl rand -base64 32
# 修改后已保存的插件凭据将无法解密,请保持稳定并妥善保管
PROVIDER_CREDENTIAL_KEY=